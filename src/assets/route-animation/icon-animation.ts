import { transition, trigger, useAnimation } from '@angular/animations'

export const IconAnimation =[
  // trigger('publication-like-standard', [
  //   transition('* => true', useAnimation(pulse,{ params: { timing: 0.5 }})),
  //   transition('* => false', useAnimation(pulse,{ params: { timing: 0.5 }}))
  // ]),
  // trigger('btn-open-publication', [
  //   transition('true <=> false', useAnimation(tada,{ params: { timing: 1.5 }}))
  // ]),
  // trigger('donation', [
  //   transition('* => true', useAnimation(tada,{ params: { timing: 1 }}))
  // ])
]
